from tkinter import *
from tkinter import ttk, filedialog


window = Tk()
window.title('SanBot Application Importer')

#folderButton = tk.Button(window, text="Pick Folder", width=25, command=)
#window.filename = filedialog.askdirectory(title="What is your folder for all your projects", initialdir="/")

window.mainloop()
